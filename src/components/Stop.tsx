import React, { useEffect, useRef, useState } from "react";
import { StopModel } from "../models";

interface Props {
  stop: StopModel;
}

const Stop = ({ stop }: Props) => {
  return <form></form>;
};

export default Stop;
